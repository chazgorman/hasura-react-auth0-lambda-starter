{
  "name": "project-common",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "scripts": {
    "all": "make setup_env && concurrently  --kill-others-on-fail --names REACT,HASURA,HASURA_CONSOLE,NGROK,SLS \"make react_start\" \"make hasura_up_delay\" \"make hasura_console_delay\" \"make ngrok_silent\" \"make sls_local\""
  }
}
